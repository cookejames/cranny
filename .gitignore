.DS_Store
node_modules/
dist/
coverage/
*.tsbuildinfo
.terraform/
*.tfstate
*.tfstate.*
# Names the Terraform state bucket, which includes the AWS account ID. Generate it from
# infra/bootstrap (see infra/backend.hcl.example).
infra/backend.hcl
*.tfplan
# Local secrets, e.g. ABLY_KEY. .env.development is tracked (no secrets).
.env
.env.local
.env.*.local
